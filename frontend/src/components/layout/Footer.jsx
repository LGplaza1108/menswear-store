const Footer = () => (
  <footer className="border-t border-stone-200 py-8">
    <div className="container-shell flex flex-col gap-3 text-sm text-stone-500 md:flex-row md:items-center md:justify-between">
      <p>Minimal menswear essentials, built for fast browsing and practical checkout.</p>
      <p>Payments via Razorpay test mode. Ready for Vercel + Render deployment.</p>
    </div>
  </footer>
);

export default Footer;
